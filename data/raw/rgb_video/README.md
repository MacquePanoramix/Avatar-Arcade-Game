# RGB Video Captures

Optional RGB videos can be stored to match OpenPose JSON takes.

- Matching optional video for each take goes in this tree.
- Suggested take naming: `take_001`, `take_002`, etc.
- Keep one person in frame for each take.
- Start and end every take in a neutral pose.
- The corresponding OpenPose frames should live under `data/raw/openpose_json`.
